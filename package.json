{
  "name": "@assert-click/runner",
  "version": "1.0.0",
  "description": "Self-hosted runner for Assert — polls for test jobs and executes them with Playwright on your own infrastructure",
  "license": "MIT",
  "homepage": "https://assert.click",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pixel-Funnel/assert-runner.git"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "assert": "bin/assert.js",
    "assert-runner": "bin/assert-runner.js"
  },
  "files": [
    "bin/",
    "src/cli.js",
    "src/executor.js"
  ],
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.0.0",
    "playwright": "^1.40.0"
  },
  "scripts": {
    "postinstall": "node -e \"require('child_process').execSync('npx playwright install chromium --with-deps', { stdio: 'inherit' })\" || true"
  },
  "publishConfig": {
    "access": "public"
  }
}
